<UserControl x:Class="RemoteFileManager.Client.Views.ServerScanDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Width="350" Height="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="Quét Server trong mạng LAN" 
                   Style="{StaticResource MaterialDesignHeadline6TextBlock}" 
                   Margin="0,0,0,10"/>

        <!-- Danh sách IP tìm được -->
        <Border Grid.Row="1" BorderBrush="LightGray" BorderThickness="1" CornerRadius="5" Margin="0,0,0,20">
            <ListBox x:Name="ServerListBox" 
                     ItemsSource="{Binding FoundServers}"
                     SelectionMode="Single">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="5">
                            <materialDesign:PackIcon Kind="ServerNetwork" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBlock Text="{Binding}" FontWeight="Bold" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <!-- Loading Animation -->
        <ProgressBar Grid.Row="1" IsIndeterminate="True" 
                     VerticalAlignment="Bottom" Height="5" 
                     Background="Transparent" BorderThickness="0"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Style="{StaticResource MaterialDesignFlatButton}"
                    IsCancel="True" Margin="0,0,10,0"
                    Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}">
                HỦY BỎ
            </Button>

            <!-- Nút Chọn: Trả về IP đang được chọn trong ListBox -->
            <Button Style="{StaticResource MaterialDesignFlatButton}"
                    IsDefault="True"
                    Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                    CommandParameter="{Binding SelectedItem, ElementName=ServerListBox}">
                CHỌN SERVER
            </Button>
        </StackPanel>
    </Grid>
</UserControl>